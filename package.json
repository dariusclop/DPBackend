{
  "name": "dpbackend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js --exec babel-node --presets env",
    "start-windows": "nodemon server.js && set GOOGLE_APPLICATION_CREDENTIALS=serverKey.json && --exec babel-node --presets env",
    "start-mac": "nodemon server.js && export GOOGLE_APPLICATION_CREDENTIALS=serverKey.json && --exec babel-node --presets env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "@google-cloud/storage": "^4.6.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase": "^7.12.0",
    "firebase-admin": "^8.10.0",
    "fs": "0.0.1-security",
    "http": "0.0.0",
    "multer": "^1.4.2",
    "path": "^0.12.7",
    "uuid": "^7.0.2"
  }
}
